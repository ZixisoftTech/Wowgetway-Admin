# Setup & Installation Guide

1. **Clone the Project**
2. **Environment Configuration:** Fill in all secrets in `backend/.env`
3. **Boot Database:** Setup local MongoDB instance or hook MongoDB Atlas URI.
4. **Boot Backend:** `npm install && npm start` inside `backend/`
5. **Boot Frontend:** `npm install && npm run dev` inside `frontend/`
